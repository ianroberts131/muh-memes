<div class="col-md-6 col-md-offset-3 center">  
  <div class="image">
    <%= image_tag @meme.image.url if @meme.image? %>
  
    <span class='delete-button'>
      <% if @user = current_user %>
        <%= link_to "Delete", user_meme_path(@user, @meme), method: :delete %>
      <% end %>
    </span>
  </div>
</div>